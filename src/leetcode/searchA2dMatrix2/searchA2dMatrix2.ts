// https://leetcode-cn.com/problems/search-a-2d-matrix-ii/
export function searchMatrix(matrix: number[][], target: number): boolean {}

const findTargetInSlope = (matrix: number[][], target: number) => {
  let x = 0,
    y = 0
  
}
